import './Header.css';

function Header() {
    return (
        <header className="app-header">
            <img className="app-header-logo" src="/images/logo.jpg" />
        </header>
    );
}

export default Header;